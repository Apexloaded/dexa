export interface IStreamCredentials {
  ingressId: string;
  serverUrl: string;
  streamKey: string;
}

export interface IStreamViewerCredentials {
  name: string;
  token: string;
  identity: string;
}
